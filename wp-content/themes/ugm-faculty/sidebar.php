<?php
/**
 * The sidebar containing the main widget area.
 *
 * This theme does not use a sidebar in its primary layout, but this file
 * is provided as a standard WordPress fallback so that plugins calling
 * get_sidebar() do not encounter a missing-template warning.
 *
 * @link https://developer.wordpress.org/themes/basics/template-files/#template-partials
 *
 * @package ugm-faculty
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
